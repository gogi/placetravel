= simple_form_for(place) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :url
    = f.input :description
    = f.input :price

  .form-actions
    = f.button :submit
